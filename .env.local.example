# Copy this file to .env.local and fill in the values.
# These are SERVER-SIDE only — never exposed to the browser.

# Password to access the app (you'll be prompted to enter this on the login page)
APP_PASSWORD=change-me-to-a-strong-password
